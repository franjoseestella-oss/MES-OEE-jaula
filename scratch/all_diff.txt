diff --git a/backend/database/repositories.py b/backend/database/repositories.py
index 32d9894..b7bf2c0 100644
--- a/backend/database/repositories.py
+++ b/backend/database/repositories.py
@@ -31,7 +31,10 @@ def upsert_machine_status(db: Session, machine_id: str, state: str,
 
 def save_event(db: Session, machine_id: str, state: str, ts: datetime,
                piece_count: int, good_count: int, bad_count: int,
-               reason_code: Optional[str] = None, source: str = "mqtt") -> MachineEvent:
+               reason_code: Optional[str] = None, source: str = "mqtt",
+               secuencia_id: Optional[str] = None, tiempo_teorico_s: Optional[int] = None,
+               duracion_real_s: Optional[int] = None, dentro_de_tiempo: Optional[bool] = None,
+               error: Optional[str] = None) -> MachineEvent:
     ev = MachineEvent(
         machine_id=machine_id,
         state=state,
@@ -41,6 +44,11 @@ def save_event(db: Session, machine_id: str, state: str, ts: datetime,
         bad_count=bad_count,
         reason_code=reason_code,
         source=source,
+        secuencia_id=secuencia_id,
+        tiempo_teorico_s=tiempo_teorico_s,
+        duracion_real_s=duracion_real_s,
+        dentro_de_tiempo=dentro_de_tiempo,
+        error=error,
     )
     db.add(ev)
     upsert_machine_status(db, machine_id, state, ts, piece_count, good_count, bad_count)
diff --git a/backend/main.py b/backend/main.py
index d2c8afa..710cb6c 100644
--- a/backend/main.py
+++ b/backend/main.py
@@ -35,13 +35,27 @@ _mqtt_client = None
 def handle_mqtt_event(payload: dict) -> None:
     """Callback invocado por el cliente MQTT al recibir un evento."""
     try:
-        machine_id = payload.get("machine_id", "UNKNOWN")
-        state = payload.get("state", "Unknown")
-        ts_raw = payload.get("timestamp", "")
+        machine_id = payload.get("machine_id") or payload.get("jaula_id") or "UNKNOWN"
+        state = payload.get("state") or payload.get("estado") or "Unknown"
+        ts_raw = payload.get("timestamp") or payload.get("ts") or ""
         ts = datetime.fromisoformat(ts_raw) if ts_raw else datetime.now(timezone.utc)
         if ts.tzinfo is None:
             ts = ts.replace(tzinfo=timezone.utc)
 
+        # Extraer campos de secuencia
+        secuencia_id = payload.get("secuencia_id")
+        tiempo_teorico_s = payload.get("tiempo_teorico_s")
+        duracion_real_s = payload.get("duracion_real_s")
+        dentro_de_tiempo = payload.get("dentro_de_tiempo")
+        error_val = payload.get("error")
+        
+        error_str = None
+        if error_val is not None:
+            if isinstance(error_val, dict):
+                error_str = json.dumps(error_val)
+            else:
+                error_str = str(error_val)
+
         with get_db() as db:
             repo.save_event(
                 db=db,
@@ -53,8 +67,13 @@ def handle_mqtt_event(payload: dict) -> None:
                 bad_count=int(payload.get("bad_count", 0)),
                 reason_code=payload.get("reason_code"),
                 source="mqtt",
+                secuencia_id=secuencia_id,
+                tiempo_teorico_s=tiempo_teorico_s,
+                duracion_real_s=duracion_real_s,
+                dentro_de_tiempo=dentro_de_tiempo,
+                error=error_str,
             )
-        logger.debug("Evento guardado: %s → %s @ %s", machine_id, state, ts)
+        logger.debug("Evento guardado: %s → %s (Secuencia: %s) @ %s", machine_id, state, secuencia_id, ts)
     except Exception as exc:
         logger.error("Error al guardar evento MQTT: %s", exc, exc_info=True)
 
diff --git a/backend/models/events.py b/backend/models/events.py
index f384c3e..c25e667 100644
--- a/backend/models/events.py
+++ b/backend/models/events.py
@@ -25,6 +25,11 @@ class MachineEvent(Base):
     bad_count = Column(Integer, default=0)
     reason_code = Column(String(50), nullable=True)
     source = Column(String(10), default="mqtt")         # "mqtt" | "sql"
+    secuencia_id = Column(String(50), nullable=True, index=True)
+    tiempo_teorico_s = Column(Integer, nullable=True)
+    duracion_real_s = Column(Integer, nullable=True)
+    dentro_de_tiempo = Column(Boolean, nullable=True)
+    error = Column(String(255), nullable=True)
     created_at = Column(DateTime, default=datetime.utcnow)
 
     __table_args__ = (
diff --git a/grafana/provisioning/dashboards/alarmas_dashboard.json b/grafana/provisioning/dashboards/alarmas_dashboard.json
index c3275e8..93cfbbf 100644
--- a/grafana/provisioning/dashboards/alarmas_dashboard.json
+++ b/grafana/provisioning/dashboards/alarmas_dashboard.json
@@ -339,6 +339,6 @@
   "timezone": "browser",
   "title": "LOGISNEXT — ALARMAS",
   "uid": "mes-alarms-v1",
-  "version": 31,
+  "version": 32,
   "weekStart": ""
 }
\ No newline at end of file
diff --git a/grafana/provisioning/dashboards/distribuidor_dashboard.json b/grafana/provisioning/dashboards/distribuidor_dashboard.json
index ed48fef..856545b 100644
--- a/grafana/provisioning/dashboards/distribuidor_dashboard.json
+++ b/grafana/provisioning/dashboards/distribuidor_dashboard.json
@@ -920,6 +920,6 @@
   "timezone": "",
   "title": "LOGISNEXT — DISTRIBUIDOR",
   "uid": "mes-oee-v2",
-  "version": 169,
+  "version": 172,
   "weekStart": ""
 }
\ No newline at end of file
diff --git a/grafana/provisioning/dashboards/home_dashboard.json b/grafana/provisioning/dashboards/home_dashboard.json
index aa6598f..e119b07 100644
--- a/grafana/provisioning/dashboards/home_dashboard.json
+++ b/grafana/provisioning/dashboards/home_dashboard.json
@@ -124,6 +124,6 @@
   "timezone": "browser",
   "title": "LOGISNEXT — INICIO",
   "uid": "mes-home-v1",
-  "version": 38,
+  "version": 39,
   "weekStart": ""
 }
\ No newline at end of file
diff --git a/grafana/provisioning/dashboards/plan_dashboard.json b/grafana/provisioning/dashboards/plan_dashboard.json
index 687cd73..0369b44 100644
--- a/grafana/provisioning/dashboards/plan_dashboard.json
+++ b/grafana/provisioning/dashboards/plan_dashboard.json
@@ -589,7 +589,7 @@
         "h": 11,
         "w": 12,
         "x": 0,
-        "y": 28
+        "y": 48
       },
       "id": 7,
       "options": {
@@ -627,7 +627,7 @@
         "h": 3,
         "w": 2,
         "x": 0,
-        "y": 39
+        "y": 59
       },
       "id": 6,
       "options": {
@@ -672,7 +672,7 @@
         "h": 11,
         "w": 12,
         "x": 12,
-        "y": 28
+        "y": 48
       },
       "id": 8,
       "options": {
@@ -773,6 +773,104 @@
       ],
       "title": "Línea de Tiempo de Estados",
       "type": "state-timeline"
+    },
+    {
+      "datasource": {
+        "type": "mssql",
+        "uid": "mes_sqlserver"
+      },
+      "fieldConfig": {
+        "defaults": {
+          "color": {
+            "mode": "thresholds"
+          },
+          "custom": {
+            "fillOpacity": 80,
+            "lineWidth": 0
+          },
+          "mappings": [
+            {
+              "options": {
+                "SECUENCIA_INICIADA": {
+                  "color": "#2FD06A",
+                  "index": 0,
+                  "text": "SECUENCIA_INICIADA"
+                },
+                "EN_PROCESO": {
+                  "color": "#2FD06A",
+                  "index": 1,
+                  "text": "EN_PROCESO"
+                },
+                "EN_ESPERA": {
+                  "color": "#FADE2A",
+                  "index": 2,
+                  "text": "EN_ESPERA"
+                },
+                "PAUSADA": {
+                  "color": "#FADE2A",
+                  "index": 3,
+                  "text": "PAUSADA"
+                },
+                "FINALIZADA": {
+                  "color": "#E5C158",
+                  "index": 4,
+                  "text": "FINALIZADA"
+                },
+                "ERROR": {
+                  "color": "#E32636",
+                  "index": 5,
+                  "text": "ERROR"
+                }
+              },
+              "type": "value"
+            }
+          ]
+        }
+      },
+      "gridPos": {
+        "h": 20,
+        "w": 24,
+        "x": 0,
+        "y": 28
+      },
+      "id": 10,
+      "options": {
+        "alignValue": "left",
+        "mergeValues": true,
+        "showValue": "never"
+      },
+      "targets": [
+        {
+          "datasource": {
+            "type": "mssql",
+            "uid": "mes_sqlserver"
+          },
+          "editorMode": "code",
+          "format": "table",
+          "rawQuery": true,
+          "rawSql": "DECLARE @ActiveDate VARCHAR(8);\nSET @ActiveDate = CONVERT(varchar(8), CAST($__timeFrom() AS DATE), 112);\n\nIF NOT EXISTS (SELECT 1 FROM JAULA_ERP WHERE fecha_montaje = @ActiveDate)\nBEGIN\n    SET @ActiveDate = COALESCE(\n        (SELECT TOP 1 CONVERT(varchar(8), FECHA_MONTAJE, 112) FROM LOG_TABLA WHERE TRY_CAST(FECHA_HORA_INICIO_SEC AS DATE) = CAST(GETDATE() AS DATE) ORDER BY id DESC),\n        (SELECT MIN(fecha_montaje) FROM JAULA_ERP WHERE fecha_montaje >= @ActiveDate),\n        (SELECT MAX(fecha_montaje) FROM JAULA_ERP WHERE fecha_montaje <= @ActiveDate),\n        (SELECT MAX(fecha_montaje) FROM JAULA_ERP)\n    );\nEND;\n\nDECLARE @SelectedDate DATE = TRY_CAST(@ActiveDate AS DATE);\nDECLARE @Monday DATE = DATEADD(wk, DATEDIFF(wk, 0, @SelectedDate), 0);\nDECLARE @Friday DATE = DATEADD(day, 4, @Monday);\n\n-- Create weekly schedule slots table\nIF OBJECT_ID('tempdb..#WeeklyScheduleSlots') IS NOT NULL DROP TABLE #WeeklyScheduleSlots;\nCREATE TABLE #WeeklyScheduleSlots (\n    fecha DATE,\n    slot_idx INT,\n    horario TIME,\n    PRIMARY KEY (fecha, slot_idx)\n);\n\nDECLARE @LoopDate DATE = @Monday;\nWHILE @LoopDate <= @Friday\nBEGIN\n    DECLARE @DayCap DECIMAL(4,1);\n    SELECT @DayCap = COALESCE(Cant_A_Fabricar, 18.0)\n    FROM dbo.CALENDARIO_LABORAL\n    WHERE Fecha = @LoopDate;\n\n    IF @DayCap IS NULL SET @DayCap = 18.0;\n\n    IF @DayCap = 18.0\n    BEGIN\n        INSERT INTO #WeeklyScheduleSlots (fecha, slot_idx, horario)\n        SELECT @LoopDate, ROW_NUMBER() OVER (ORDER BY id), horario\n        FROM dbo.HHSS_18;\n    END\n    ELSE IF @DayCap = 19.0\n    BEGIN\n        INSERT INTO #WeeklyScheduleSlots (fecha, slot_idx, horario)\n        SELECT @LoopDate, ROW_NUMBER() OVER (ORDER BY id), horario\n        FROM dbo.HHSS_19;\n    END\n    ELSE IF @DayCap = 18.5\n    BEGIN\n        DECLARE @Cnt185 INT = 0;\n        SELECT @Cnt185 = COUNT(*)\n        FROM dbo.CALENDARIO_LABORAL\n        WHERE Laborable = 1 \n          AND Cant_A_Fabricar = 18.5 \n          AND Fecha <= @LoopDate;\n\n        IF @Cnt185 > 0 AND @Cnt185 % 2 = 0\n        BEGIN\n            INSERT INTO #WeeklyScheduleSlots (fecha, slot_idx, horario)\n            SELECT @LoopDate, ROW_NUMBER() OVER (ORDER BY id), horario\n            FROM dbo.HHSS_18_5\n            WHERE id BETWEEN 20 AND 38;\n        END\n        ELSE\n        BEGIN\n            INSERT INTO #WeeklyScheduleSlots (fecha, slot_idx, horario)\n            SELECT @LoopDate, ROW_NUMBER() OVER (ORDER BY id), horario\n            FROM dbo.HHSS_18_5\n            WHERE id BETWEEN 1 AND 19;\n        END\n    END\n    ELSE\n    BEGIN\n        INSERT INTO #WeeklyScheduleSlots (fecha, slot_idx, horario)\n        SELECT @LoopDate, ROW_NUMBER() OVER (ORDER BY id), horario\n        FROM dbo.HHSS_18;\n    END;\n\n    SET @LoopDate = DATEADD(day, 1, @LoopDate);\nEND;\n\n-- Get all sequences of the week\nIF OBJECT_ID('tempdb..#SeqsToSchedule') IS NOT NULL DROP TABLE #SeqsToSchedule;\nCREATE TABLE #SeqsToSchedule (\n    id INT PRIMARY KEY,\n    secuencia VARCHAR(50),\n    bastidor VARCHAR(50),\n    modelo VARCHAR(50),\n    original_date DATE,\n    completed BIT DEFAULT 0,\n    status VARCHAR(50) DEFAULT 'Pendiente',\n    completion_time DATETIME,\n    actual_date DATE,\n    scheduled_date DATE,\n    slot_idx INT,\n    planned_start DATETIME,\n    planned_end DATETIME\n);\n\nINSERT INTO #SeqsToSchedule (id, secuencia, bastidor, modelo, original_date, completed, status, completion_time, actual_date)\nSELECT \n    erp.id,\n    erp.secuencia,\n    erp.bastidor,\n    erp.modelo,\n    TRY_CAST(erp.fecha_montaje AS DATE),\n    CASE WHEN log.id IS NOT NULL THEN 1 ELSE 0 END,\n    COALESCE(log.OK_NOK, 'Pendiente'),\n    log.FECHA_HORA_FIN_SEC,\n    TRY_CAST(CAST(COALESCE(log.FECHA_HORA_FIN_SEC, log.FECHA_HORA_INICIO_SEC) AS datetime2) AT TIME ZONE 'UTC' AT TIME ZONE 'Romance Standard Time' AS DATE)\nFROM dbo.JAULA_ERP erp\nLEFT JOIN (\n    SELECT \n        id,\n        NBASTIDOR,\n        FECHA_MONTAJE,\n        OK_NOK,\n        FECHA_HORA_INICIO_SEC,\n        FECHA_HORA_FIN_SEC,\n        ROW_NUMBER() OVER (PARTITION BY NBASTIDOR, FECHA_MONTAJE ORDER BY id DESC) as rn\n    FROM dbo.LOG_TABLA\n) log ON log.NBASTIDOR = erp.bastidor AND log.FECHA_MONTAJE = TRY_CAST(erp.fecha_montaje AS DATE) AND log.rn = 1\nWHERE \n    (TRY_CAST(erp.fecha_montaje AS DATE) BETWEEN @Monday AND @Friday)\n    OR (TRY_CAST(erp.fecha_montaje AS DATE) < @Monday AND log.id IS NULL)\n    OR (TRY_CAST(erp.fecha_montaje AS DATE) < @Monday \n        AND log.id IS NOT NULL \n        AND TRY_CAST(CAST(COALESCE(log.FECHA_HORA_FIN_SEC, log.FECHA_HORA_INICIO_SEC) AS datetime2) AT TIME ZONE 'UTC' AT TIME ZONE 'Romance Standard Time' AS DATE) BETWEEN @Monday AND @Friday);\n\n-- First, any sequence that was completed is scheduled on the day it actually ran\nUPDATE #SeqsToSchedule\nSET scheduled_date = actual_date\nWHERE completed = 1 AND actual_date IS NOT NULL;\n\n-- Now we loop day-by-day to schedule pending sequences\nDECLARE @CurrDate DATE = @Monday;\nDECLARE @Today DATE = CAST(GETDATE() AS DATE);\n\nWHILE @CurrDate <= @Friday\nBEGIN\n    DECLARE @Cap INT;\n    SELECT @Cap = COUNT(*) FROM #WeeklyScheduleSlots WHERE fecha = @CurrDate;\n    \n    DECLARE @Occupied INT;\n    SELECT @Occupied = COUNT(*) FROM #SeqsToSchedule WHERE scheduled_date = @CurrDate;\n    \n    DECLARE @Available INT = @Cap - @Occupied;\n    \n    IF @CurrDate >= @Today AND @Available > 0\n    BEGIN\n        WITH TodayPending AS (\n            SELECT TOP (@Available) *\n            FROM #SeqsToSchedule\n            WHERE scheduled_date IS NULL\n              AND original_date <= @CurrDate\n            ORDER BY id ASC\n        )\n        UPDATE TodayPending\n        SET scheduled_date = @CurrDate;\n    END;\n    \n    SET @CurrDate = DATEADD(day, 1, @CurrDate);\nEND;\n\n-- Any sequences still not scheduled are scheduled for next Monday (or Friday + 3)\nUPDATE #SeqsToSchedule\nSET scheduled_date = DATEADD(day, 3, @Friday)\nWHERE scheduled_date IS NULL;\n\n-- Now assign slot_idx, planned_start and planned_end\nWITH OrderedScheduled AS (\n    SELECT \n        id,\n        scheduled_date,\n        ROW_NUMBER() OVER (PARTITION BY scheduled_date ORDER BY completed DESC, id ASC) as row_idx\n    FROM #SeqsToSchedule\n)\nUPDATE s\nSET \n    s.slot_idx = os.row_idx,\n    s.planned_start = DATEADD(second, \n        CASE \n            WHEN os.row_idx = 1 THEN 0\n            ELSE DATEDIFF(second, '07:00:00', COALESCE(t_prev.horario, '07:00:00'))\n        END,\n        DATEADD(hour, 7, CAST(s.scheduled_date AS DATETIME))\n    ),\n    s.planned_end = DATEADD(second, \n        DATEDIFF(second, '07:00:00', COALESCE(t_curr.horario, '07:00:00')),\n        DATEADD(hour, 7, CAST(s.scheduled_date AS DATETIME))\n    )\nFROM #SeqsToSchedule s\nINNER JOIN OrderedScheduled os ON os.id = s.id\nLEFT JOIN #WeeklyScheduleSlots t_curr ON t_curr.fecha = s.scheduled_date AND t_curr.slot_idx = os.row_idx\nLEFT JOIN #WeeklyScheduleSlots t_prev ON t_prev.fecha = s.scheduled_date AND t_prev.slot_idx = os.row_idx - 1;\n\n-- Output for timeline\nWITH SequenceStates AS (\n    SELECT \n        secuencia,\n        planned_start,\n        planned_end,\n        (\n            SELECT TOP 1 state \n            FROM mes_machine_events \n            WHERE secuencia_id = s.secuencia \n            ORDER BY timestamp DESC\n        ) AS latest_state\n    FROM #SeqsToSchedule s\n    WHERE s.scheduled_date = @SelectedDate\n)\nSELECT time, metric, value FROM (\n    SELECT \n        planned_start AS time,\n        secuencia AS metric,\n        CASE COALESCE(latest_state, 'Idle')\n            WHEN 'Execute' THEN 'EN_PROCESO'\n            WHEN 'Starting' THEN 'SECUENCIA_INICIADA'\n            WHEN 'Running' THEN 'EN_PROCESO'\n            WHEN 'Idle' THEN 'EN_ESPERA'\n            WHEN 'Held' THEN 'PAUSADA'\n            WHEN 'Complete' THEN 'FINALIZADA'\n            WHEN 'Stopped' THEN 'FINALIZADA'\n            WHEN 'Aborted' THEN 'ERROR'\n            WHEN 'Aborting' THEN 'ERROR'\n            WHEN 'EN_PROCESO' THEN 'EN_PROCESO'\n            WHEN 'SECUENCIA_INICIADA' THEN 'SECUENCIA_INICIADA'\n            WHEN 'EN_ESPERA' THEN 'EN_ESPERA'\n            WHEN 'PAUSADA' THEN 'PAUSADA'\n            WHEN 'FINALIZADA' THEN 'FINALIZADA'\n            WHEN 'ERROR' THEN 'ERROR'\n            ELSE 'EN_ESPERA'\n        END AS value\n    FROM SequenceStates\n    \n    UNION ALL\n    \n    SELECT \n        planned_end AS time,\n        secuencia AS metric,\n        NULL AS value\n    FROM SequenceStates\n) t\nORDER BY metric ASC, time ASC;",
+          "refId": "A"
+        }
+      ],
+      "title": "Plan de Producción por Secuencias (Teórico vs Real)",
+      "type": "state-timeline",
+      "transformations": [
+        {
+          "id": "partitionByValues",
+          "options": {
+            "fields": [
+              "metric"
+            ]
+          }
+        },
+        {
+          "id": "renameByRegex",
+          "options": {
+            "regex": "^value\\s(.+)",
+            "renamePattern": "$1"
+          }
+        }
+      ]
     }
   ],
   "refresh": "5s",
diff --git a/grafana/provisioning/dashboards/registro_dashboard.json b/grafana/provisioning/dashboards/registro_dashboard.json
index 7d84adb..ce76327 100644
--- a/grafana/provisioning/dashboards/registro_dashboard.json
+++ b/grafana/provisioning/dashboards/registro_dashboard.json
@@ -840,6 +840,6 @@
   "timezone": "browser",
   "title": "LOGISNEXT — REGISTRO",
   "uid": "mes-reg-v1",
-  "version": 66,
+  "version": 71,
   "weekStart": ""
 }
\ No newline at end of file
diff --git a/grafana/provisioning/dashboards/turno_actual_dashboard.json b/grafana/provisioning/dashboards/turno_actual_dashboard.json
index 6222ebb..88a1c4b 100644
--- a/grafana/provisioning/dashboards/turno_actual_dashboard.json
+++ b/grafana/provisioning/dashboards/turno_actual_dashboard.json
@@ -661,5 +661,5 @@
   "timezone": "browser",
   "title": "Panel de Control OEE Fábrica - Turno Actual (ES)",
   "uid": "panel-oee-mes-fabrica",
-  "version": 20
+  "version": 21
 }
\ No newline at end of file
diff --git a/scratch/inspect_db_dates.py b/scratch/inspect_db_dates.py
index 7408a95..9c9969f 100644
--- a/scratch/inspect_db_dates.py
+++ b/scratch/inspect_db_dates.py
@@ -1,39 +1,30 @@
 import pyodbc
+import sys
+
+sys.stdout.reconfigure(encoding='utf-8')
 
 conn_str = (
-    "DRIVER={ODBC Driver 17 for SQL Server};"
-    "SERVER=DESKTOP-PMRMSPT\\SQLEXPRESS,1435;"
-    "DATABASE=DAFEED;"
-    "UID=usuario_readonly;"
-    "PWD=Logisnext2026!;"
+    "Driver={ODBC Driver 17 for SQL Server};"
+    "Server=DESKTOP-PMRMSPT\\SQLEXPRESS;"
+    "Database=DAFEED;"
+    "Trusted_Connection=yes;"
     "TrustServerCertificate=yes;"
-    "ConnLifetime=30;"
 )
 
 try:
     conn = pyodbc.connect(conn_str)
     cursor = conn.cursor()
-    
-    print("--- DISTINCT fecha_montaje in JAULA_ERP ---")
-    cursor.execute("SELECT DISTINCT fecha_montaje FROM JAULA_ERP ORDER BY fecha_montaje DESC")
-    for row in cursor.fetchall():
-        print(row[0])
-        
-    print("\n--- COUNT OF RECORDS IN JAULA_ERP FOR '20260612' ---")
-    cursor.execute("SELECT COUNT(*) FROM JAULA_ERP WHERE fecha_montaje = '20260612'")
-    print(cursor.fetchone()[0])
-    
-    print("\n--- COUNT OF RECORDS IN LOG_TABLA FOR '20260612' ---")
-    cursor.execute("SELECT COUNT(*) FROM LOG_TABLA WHERE FECHA_MONTAJE = '20260612'")
-    print(cursor.fetchone()[0])
-    
-    print("\n--- RECENT ENTRIES IN LOG_TABLA ---")
-    cursor.execute("SELECT TOP 5 FECHA_MONTAJE, fecha_creacion, OK_NOK FROM LOG_TABLA ORDER BY id DESC")
-    for row in cursor.fetchall():
-        print(row)
-        
 except Exception as e:
-    print(f"Error: {e}")
-finally:
-    if 'conn' in locals():
-        conn.close()
+    print(f"Error connecting: {e}")
+    sys.exit(1)
+
+cursor.execute("SELECT GETDATE(), CAST(GETDATE() AS DATE)")
+row = cursor.fetchone()
+print(f"GETDATE(): {row[0]}, CAST AS DATE: {row[1]}")
+
+cursor.execute("SELECT TOP 5 FECHA_MONTAJE, FECHA_HORA_INICIO_SEC FROM LOG_TABLA ORDER BY id DESC")
+print("Latest rows in LOG_TABLA:")
+for r in cursor.fetchall():
+    print(f"FECHA_MONTAJE: {r[0]}, FECHA_HORA_INICIO_SEC: {r[1]}")
+
+conn.close()
diff --git a/scratch/inspect_layout.py b/scratch/inspect_layout.py
index d0e865f..22c9795 100644
--- a/scratch/inspect_layout.py
+++ b/scratch/inspect_layout.py
@@ -1,12 +1,16 @@
 import json
 import sys
 
-sys.stdout.reconfigure(encoding='utf-8')
+# Ensure UTF-8 output
+if hasattr(sys.stdout, 'reconfigure'):
+    sys.stdout.reconfigure(encoding='utf-8')
 
 dashboard_path = r"c:\Users\franj\OneDrive\Escritorio\COSAS  FRAN\PROYECTOS\JAULA ELEVACION\APLICACION MES-OEE\grafana\provisioning\dashboards\plan_dashboard.json"
 
-with open(dashboard_path, "r", encoding="utf-8") as f:
-    db = json.load(f)
+with open(dashboard_path, 'r', encoding='utf-8') as f:
+    data = json.load(f)
 
-for panel in db.get("panels", []):
-    print(f"ID: {panel.get('id')} | Title: {panel.get('title')} | Type: {panel.get('type')} | GridPos: {panel.get('gridPos')}")
+print("Title:", data.get("title"))
+print("UID:", data.get("uid"))
+for panel in data.get("panels", []):
+    print(f"Panel ID: {panel.get('id')} - Title: {panel.get('title')} - Type: {panel.get('type')} - GridPos: {panel.get('gridPos')}")
diff --git a/scratch/print_panel_9.py b/scratch/print_panel_9.py
index ede2ece..92cd775 100644
--- a/scratch/print_panel_9.py
+++ b/scratch/print_panel_9.py
@@ -1,9 +1,10 @@
 import json
 
-with open("grafana/provisioning/dashboards/log_dashboard.json", "r", encoding="utf-8") as f:
+path = r"c:\Users\franj\OneDrive\Escritorio\COSAS  FRAN\PROYECTOS\JAULA ELEVACION\APLICACION MES-OEE\grafana\provisioning\dashboards\plan_dashboard.json"
+with open(path, "r", encoding="utf-8") as f:
     db = json.load(f)
 
 for p in db.get("panels", []):
     if p.get("id") == 9:
-        print(json.dumps(p, indent=2, ensure_ascii=False))
+        print(json.dumps(p, indent=2))
         break
diff --git a/scratch/seed_oee_demo_data.py b/scratch/seed_oee_demo_data.py
index 2a06c25..9d3520e 100644
--- a/scratch/seed_oee_demo_data.py
+++ b/scratch/seed_oee_demo_data.py
@@ -114,6 +114,20 @@ def seed_machine_events():
     now = datetime.datetime.now()
     end = min(SHIFT_END, now)
 
+    # Fetch today's sequences to map them
+    active_date_erp = TODAY.strftime("%Y%m%d")
+    res = grafana_sql(f"SELECT secuencia FROM dbo.JAULA_ERP WHERE fecha_montaje = '{active_date_erp}' ORDER BY id")
+    seq_list = []
+    try:
+        if res and "results" in res:
+            frames = res["results"]["A"]["frames"]
+            if frames and len(frames) > 0:
+                data = frames[0]["data"]
+                if data and "values" in data and len(data["values"]) > 0:
+                    seq_list = data["values"][0]
+    except Exception as e:
+        print(f"  [WARNING] Could not fetch sequences for today: {e}")
+
     while t < end:
         # Lunch break
         if BREAK_START <= t < BREAK_END:
@@ -166,10 +180,20 @@ def seed_machine_events():
                 "Mantenimiento Preventivo", "Error Operario"
             ])
 
+        # Map to today's sequences based on time elapsed in the shift
+        seq_id = None
+        if seq_list:
+            shift_duration = (SHIFT_END - SHIFT_START).total_seconds()
+            elapsed = (t - SHIFT_START).total_seconds()
+            seq_idx = int(elapsed / shift_duration * len(seq_list))
+            seq_idx = min(max(0, seq_idx), len(seq_list) - 1)
+            seq_id = seq_list[seq_idx]
+
         events.append({
             "state": state, "state_code": code,
             "start": t, "end": ev_end,
-            "pieces": pieces, "good": good, "bad": bad, "reason": reason
+            "pieces": pieces, "good": good, "bad": bad, "reason": reason,
+            "secuencia_id": seq_id
         })
         t = ev_end
 
@@ -179,11 +203,12 @@ def seed_machine_events():
         total_good += ev["good"]
         total_bad += ev["bad"]
         reason_val = f"'{ev['reason']}'" if ev["reason"] else "NULL"
+        seq_val = f"'{ev['secuencia_id']}'" if ev.get("secuencia_id") else "NULL"
         grafana_sql(f"""
             INSERT INTO mes_machine_events
-            (machine_id, state, timestamp, piece_count, good_count, bad_count, reason_code, source, created_at)
+            (machine_id, state, timestamp, piece_count, good_count, bad_count, reason_code, source, created_at, secuencia_id)
             VALUES ('{MACHINE_ID}', '{ev["state"]}', '{ev["start"].strftime(FMT)}',
-                    {ev["pieces"]}, {ev["good"]}, {ev["bad"]}, {reason_val}, 'seed', GETDATE())
+                    {ev["pieces"]}, {ev["good"]}, {ev["bad"]}, {reason_val}, 'seed', GETDATE(), {seq_val})
         """)
 
     print(f"  [OK] Inserted {len(events)} machine events")
@@ -304,6 +329,90 @@ def update_machine_status(total_good, total_bad):
     print("  [OK] Updated machine status")
 
 
+def ensure_today_setup():
+    """Ensure today is set up in CALENDARIO_LABORAL and JAULA_ERP."""
+    active_date_str = TODAY.strftime("%Y-%m-%d")
+    active_date_erp = TODAY.strftime("%Y%m%d")
+
+    print(f"  [INFO] Ensuring calendar entry for today ({active_date_str})...")
+    # Check if entry exists
+    cal_res = grafana_sql(f"SELECT COUNT(*) as cnt FROM dbo.CALENDARIO_LABORAL WHERE Fecha = '{active_date_str}'")
+    cal_count = 0
+    try:
+        if cal_res and "results" in cal_res:
+            frames = cal_res["results"]["A"]["frames"]
+            if frames and len(frames) > 0:
+                data = frames[0]["data"]
+                if data and "values" in data and len(data["values"]) > 0:
+                    cal_count = data["values"][0][0]
+    except Exception as e:
+        print(f"  [WARNING] Could not parse CALENDARIO_LABORAL check results: {e}")
+
+    if cal_count > 0:
+        # Update existing
+        grafana_sql(f"UPDATE dbo.CALENDARIO_LABORAL SET Laborable = 1, Cant_A_Fabricar = 18, Tipo_Dia = 'Laborable' WHERE Fecha = '{active_date_str}'")
+        print(f"  [OK] Updated today's calendar entry to Laborable=True, Cant=18")
+    else:
+        # Insert new
+        grafana_sql(f"INSERT INTO dbo.CALENDARIO_LABORAL (Fecha, Tipo_Dia, Laborable, Cant_A_Fabricar) VALUES ('{active_date_str}', 'Laborable', 1, 18)")
+        print(f"  [OK] Inserted today's calendar entry as Laborable=True, Cant=18")
+
+    print(f"  [INFO] Ensuring sequences in JAULA_ERP for today ({active_date_erp})...")
+    erp_res = grafana_sql(f"SELECT COUNT(*) as cnt FROM dbo.JAULA_ERP WHERE fecha_montaje = '{active_date_erp}'")
+    erp_count = 0
+    try:
+        if erp_res and "results" in erp_res:
+            frames = erp_res["results"]["A"]["frames"]
+            if frames and len(frames) > 0:
+                data = frames[0]["data"]
+                if data and "values" in data and len(data["values"]) > 0:
+                    erp_count = data["values"][0][0]
+    except Exception as e:
+        print(f"  [WARNING] Could not parse JAULA_ERP check results: {e}")
+
+    if erp_count > 0:
+        print(f"  [OK] Found {erp_count} sequences in JAULA_ERP for today")
+        return
+
+    # Find latest date in JAULA_ERP
+    print("  [INFO] No sequences found for today in JAULA_ERP. Copying from latest available date...")
+    latest_res = grafana_sql(f"SELECT TOP 1 fecha_montaje FROM dbo.JAULA_ERP WHERE fecha_montaje < '{active_date_erp}' ORDER BY fecha_montaje DESC")
+    latest_date = None
+    try:
+        if latest_res and "results" in latest_res:
+            frames = latest_res["results"]["A"]["frames"]
+            if frames and len(frames) > 0:
+                data = frames[0]["data"]
+                if data and "values" in data and len(data["values"]) > 0:
+                    latest_date = data["values"][0][0]
+    except Exception as e:
+        print(f"  [WARNING] Could not parse latest JAULA_ERP date: {e}")
+
+    if not latest_date:
+        latest_date = "20260619"  # Fallback
+
+    print(f"  [INFO] Copying sequences from {latest_date} to {active_date_erp}...")
+    insert_query = f"""
+        INSERT INTO dbo.JAULA_ERP (
+            fecha_montaje, secuencia, modelo, bastidor, mastil, altura_max_interm,
+            capac_interm_1, capac_interm_2, capac_interm_3, tpo_elevac_min, tpo_elevac_max,
+            tpo_descenso_min, tpo_descenso_max, tpo_incl_adel_max, tpo_incl_atras_max,
+            tpo_elev_min_scarga, tpo_elev_max_scarga, tpo_desc_min_scarga, tpo_desc_max_scarga,
+            peso_pruebas, fecha_importacion
+        )
+        SELECT 
+            '{active_date_erp}', secuencia, modelo, CAST(bastidor AS VARCHAR(18)) + '_' + SUBSTRING('{active_date_erp}', 5, 4), mastil, altura_max_interm,
+            capac_interm_1, capac_interm_2, capac_interm_3, tpo_elevac_min, tpo_elevac_max,
+            tpo_descenso_min, tpo_descenso_max, tpo_incl_adel_max, tpo_incl_atras_max,
+            tpo_elev_min_scarga, tpo_elev_max_scarga, tpo_desc_min_scarga, tpo_desc_max_scarga,
+            peso_pruebas, GETDATE()
+        FROM dbo.JAULA_ERP
+        WHERE fecha_montaje = '{latest_date}'
+    """
+    grafana_sql(insert_query)
+    print(f"  [OK] Sequences copied successfully to today ({active_date_erp})")
+
+
 def main():
     print("=" * 60)
     print("  OEE Demo Data Seeder (via Grafana API)")
@@ -314,6 +423,9 @@ def main():
     print("\n[1/7] Ensuring helper tables...")
     ensure_tables()
 
+    print("\n[1.5/7] Ensuring today's setup in DB...")
+    ensure_today_setup()
+
     print("\n[2/7] Clearing old demo data...")
     clear_demo_data()
 
diff --git a/scratch/view_git_diff.py b/scratch/view_git_diff.py
index 1f75b74..5a05f6b 100644
--- a/scratch/view_git_diff.py
+++ b/scratch/view_git_diff.py
@@ -1,20 +1,32 @@
 import subprocess
+import sys
 
-result = subprocess.run(
-    ["git", "show", "dc98fb2", "--", "grafana/provisioning/dashboards/log_dashboard.json"],
+sys.stdout.reconfigure(encoding='utf-8')
+
+res = subprocess.run(
+    ["git", "diff", "grafana/provisioning/dashboards/plan_dashboard.json"],
     capture_output=True,
     text=True,
-    encoding="utf-8",
-    errors="ignore"
+    encoding="utf-8"
 )
 
-lines = result.stdout.splitlines()
-in_panel_15 = False
-count = 0
+# Search for options in panel 10 or orientation in the diff
+diff_text = res.stdout
+print("Is 'orientation' in the diff?", "orientation" in diff_text)
+print("Is 'vertical' in the diff?", "vertical" in diff_text)
+print("Is 'horizontal' in the diff?", "horizontal" in diff_text)
+
+# Let's save the diff of panel 10 specifically
+lines = diff_text.splitlines()
+panel_10_lines = []
+in_panel_10 = False
 for line in lines:
-    if 'id": 15' in line:
-        in_panel_15 = True
-        idx = lines.index(line)
-        for i in range(idx - 20, min(idx + 120, len(lines))):
-            print(lines[i])
-        break
+    if 'Plan de Producción' in line:
+        in_panel_10 = True
+    if in_panel_10:
+        panel_10_lines.append(line)
+        if len(panel_10_lines) > 100:
+            break
+
+print("\n--- Panel 10 Diff snippet ---")
+print("\n".join(panel_10_lines))
